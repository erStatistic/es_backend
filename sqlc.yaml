version: "2"
sql:
  - engine: "postgresql"
    schema:
      - sql/schema
    queries:
      - sql/queries
    gen:
      go:
        out: internal/database
        package: database
        sql_package: "pgx/v5" # ✅ pgx
        emit_json_tags: true
        emit_pointers_for_null_types: true
